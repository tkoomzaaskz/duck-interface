define([],
function() {

    'use strict';

    return {
        urlRoot: 'http://my-app/api/v1',
        useFakeServer: true,
        logging_enabled: true,
        logged_events: ['change', 'sync']
    };
});
