define([],
function() {

    'use strict';

    return {
        urlRoot: 'http://duck-api/',
        useFakeServer: true,
        logging_enabled: true,
        logged_events: ['change', 'sync']
    };
});
