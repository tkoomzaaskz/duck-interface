define([],
function() {

    'use strict';

    return {
        currency: 'zł',
        ajaxError: {
            'type': 'AJAX',
            'message': 'could not load data from server'
        }
    };
});
